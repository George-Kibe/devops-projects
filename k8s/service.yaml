apiVersion: v1
kind: Service
metadata:
  name: devops-projects-api-service
  labels:
    app: devops-projects-api
spec:
  selector:
    app: devops-projects-api
  ports:
    - protocol: TCP
      port: 3000 # Service port
      targetPort: 3000 # Container port
  type: NodePort # change to LoadBalancer if running in cloud
